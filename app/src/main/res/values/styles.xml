<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customer Action Bar Styles -->
        <item name="actionBarStyle">@style/dfyt.p2p.actionBarStyle</item>
        <item name="homeAsUpIndicator">@drawable/abc_ic_ab_back_material</item>

        <!--   -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--<item name="android:windowAnimationStyle">@style/activityAnim</item>-->
    </style>

    <!-- Action Bar Styles -->
    <style name="dfyt.p2p.actionBarStyle" parent="Base.Widget.AppCompat.ActionBar">
        <item name="background">@color/actionBarBg</item>
        <item name="height">40dp</item>
        <item name="titleTextStyle">@style/dfyt.p2p.actionBarStyle.Title</item>
    </style>

    <!-- Action Bar Title Styles -->
    <style name="dfyt.p2p.actionBarStyle.Title" parent="Base.TextAppearance.AppCompat.Widget.ActionBar.Title">
        <!--<item name="android:textColor">#000000</item>-->
        <item name="android:textSize">20dp</item>
    </style>

    <style name="activityAnim">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_right</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">?attr/colorAccent</item>
        <item name="tabIndicatorHeight">0dp</item>
        <!--<item name="tabPaddingStart">12dp</item>-->
        <item name="tabTextColor">#6b7984</item>
        <item name="tabPaddingEnd">12dp</item>
        <item name="tabSelectedTextColor">#f7554a</item>
    </style>

</resources>
